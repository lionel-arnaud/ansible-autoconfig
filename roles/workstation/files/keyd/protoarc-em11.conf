# Map only the ProtoArc EM11 receiver. Keyd grabs these events before Wayland,
# so Chromium receives tab shortcuts instead of native Back/Forward buttons.
[ids]
m:25a7:fa61

[main]
mouseback = C-pageup
mouseforward = C-pagedown
